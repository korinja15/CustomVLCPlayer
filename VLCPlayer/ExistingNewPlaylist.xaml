<Page x:Class="VLCPlayer.ExistingNewPlaylist"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VLCPlayer"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance local:MainWindowVM}"
      d:DesignHeight="300" d:DesignWidth="300"
      Title="ExistingNewPlaylist">

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <Button Content="Add to an existing playlist"
                    Command="{Binding SelectPlaylist_Command}"
                    Height="40"
                    Margin="10"
                    Background="#666"
                    Foreground="#eee"/>
            <Button Content="Create a new playlist"
                    Command="{Binding CreatePlaylist_Command}"
                    Height="40"
                    Margin="10"
                    Background="#666"
                    Foreground="#eee"/>
        </StackPanel>
    </Grid>
</Page>
